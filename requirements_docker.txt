# ===========================================================================
# Docker-specific requirements for CIDeconvolve
# ===========================================================================
# The exact PyTorch CUDA wheel is installed separately by Dockerfile build args.
# All versions pinned for reproducible builds.
# ===========================================================================

# Core dependencies
numpy>=1.26,<3
tifffile>=2024.1.30
imagecodecs>=2024.1.1
ome-types>=0.5
Pillow>=10.0
PyYAML>=6.0

# Benchmark system metrics
psutil>=5.9
nvidia-ml-py>=12.0.0

# OME-TIFF and OME-Zarr reading
bioio==3.2.0
bioio-ome-tiff==1.4.0
bioio-ome-zarr==3.3.0
ome-zarr>=0.16.0
zarr>=2.16
numcodecs>=0.12
